### I created this simple CLI tool because I was learning golang and wanted to build something

```bash
./main <command-name> -flags <command-input>
```
There are currently only 2 commands, "greet" and "help"
